# Codex Software Development Harness

このファイルは、Codex がこのリポジトリでソフトウェア開発を行う際の既定ルールです。システム・開発者指示とユーザーの明示的な依頼を優先し、リポジトリ内では現在地に近い `AGENTS.override.md` または `AGENTS.md` が上位ディレクトリの一般ルールを上書きします。skill は適用条件を満たす場合だけ読み、ユーザーの依頼範囲や権限を拡張しません。

## 指示の確認

- 作業開始時に、Codexが認識したプロジェクトルートと適用中の指示ファイルが意図どおりか確認する。別ディレクトリで開始した場合や指示が古く見える場合は、新しいセッションで再読込する。
- 常時読み込む `AGENTS.md` は、全作業に必要な判断だけを短く保つ。詳細な条件付き手順はskillへ分離し、命令の重複や競合を避ける。
- ルートから現在地までの指示が既定上限に収まるよう保つ。長大化した場合は、対象ディレクトリに近い指示へ分割する。

## 作業の基本

- 依頼の目的と変更範囲を先に確定する。依頼されていない機能、依存関係、外部連携、リファクタリングを追加しない。
- 作業開始前に `git status --short` と必要な範囲の `git diff` を確認し、既存の未コミット変更を保持する。`git reset --hard` や `git checkout --` で変更を捨てない。
- 最寄りの `AGENTS.md` と `README.md`、パッケージマニフェスト、対象コード、関連テストを必要な範囲だけ読む。
- 検索は `rg` / `rg --files` を優先する。リポジトリ全体を無目的に読み込まず、入口・呼び出し元・設定・テストから追跡する。
- 変更は小さく、1つの目的に集中させる。既存の構成、命名、依存関係、フォーマッタ、パッケージマネージャを尊重する。
- 新しい依存関係は、標準機能や既存依存で足りないことを確認してから追加し、必要性と選定理由を最終報告に残す。定評があり保守されているライブラリを優先する。
- ファイルを編集したら、変更箇所を再読し、差分と対象範囲を確認する。

## 標準ワークフロー

1. 依頼、制約、受け入れ条件、変更対象を短く整理する。
2. 既存コードとテストを根拠付きで調査する。未知の構成を広く把握する必要がある場合だけ `repo-research` skill を使う。
3. 必要なら最小限の実装方針を決め、ユーザーの意図を変えない範囲で実装する。
4. 変更に見合った検証を行う。関連する既存テスト、型チェック、リンター、ビルド、手動確認から必要なものを選ぶ。
5. 検証が成功したら、変更内容、検証コマンド、未解決のリスクを簡潔に報告する。失敗した場合は、失敗の根拠と次の選択肢を残す。

GitHubへの作成、同期、push、pull、Issue、Pull Requestなどを依頼された場合だけ、`skills/github-operations`を読み、その手順に従う。通常のローカル開発ではGitHubへ自動的に書き込まない。

### 検証の比例性

- 可逆で影響の小さい変更の実装をそのまま写すだけのテストは、目的と効果を検討してから追加する。
- 必要な検証を通過した後は、新しい変更、失敗、リスク、未解決の懸念がない限り、同じ検証を無目的に繰り返したり、範囲を広げたりしない。
- バグ修正、公開 API、データ移行、認証・認可、並行性、永続化、課金、セキュリティに関わる変更は、回帰を示す意味のある検証を優先する。
- テストを実行できない場合は、理由を隠さず、代わりに実施した静的確認や手動確認を明記する。

## モデル方針: Sol / Luna

Codex のモデル選択が可能な場合、既定経路を2段に保つ。

- `gpt-5.6-sol / medium`: 親エージェント、複数モジュールの実装、アーキテクチャ、デバッグ、変更影響の大きいレビュー、最終統合と判断。
- `gpt-5.6-luna / max`: 範囲の限定された探索、候補ファイルの特定、単純な変換・整形、定型チェック、独立した読み取り専用の確認。
- Luna/max が受け入れ条件を満たさない、または局所探索を越える判断が必要になった場合は、同じ失敗を繰り返し再試行せず、得られた証拠を短く引き継いで Sol/medium へ昇格する。
- Sol の `high` / `xhigh` / `max` や別モデルは既定経路に追加しない。ユーザーが明示指定した場合、または repo-local eval で測定可能な改善が確認された場合だけ使う。
- 中間モデルを静的ルーティングへ追加するより、まず Luna/max → Sol/medium の単純な昇格規則を維持する。
- ユーザー指定のモデル、reasoning、ツール、実行環境を優先する。

## トークンとサブエージェント

- 必要なファイルだけを読み、同じ内容を再取得しない。コマンド結果、差分、テスト結果を再利用する。
- 調査は「入口・証拠・次の判断」が揃った時点で止める。推測を埋めるためだけに全ファイルや全履歴を読む必要はない。
- サブエージェントは主スレッドの文脈汚染を減らし、独立作業を並列化できる一方、通常は単一エージェントより総トークンを多く使う。小規模または直列依存の作業では使わない。
- 以下をすべて満たす場合に限り、サブエージェントへの委譲を検討する。
  - 作業が独立した複数の読み取り専用タスクに分割できる。
  - 並列化により所要時間、主スレッドのコンテキスト品質、または検証品質が改善する。
  - 各タスクに対象ファイル、禁止事項、短い出力形式を指定できる。
- 既定の並列数は2。リポジトリが広い場合でも3を上限の目安とする。サブエージェントを増やす前に、分割と統合のオーバーヘッドを見積もる。
- サブエージェントの依頼は最小限のコンテキストで行い、`ファイル:行` の証拠、結論、未確定点だけを返させる。重複する要約やコード全文を返させない。
- Luna/max は低コストな探索・機械的確認、Sol/medium は設計判断・難しい検証・最終統合へ割り当てる。
- 親エージェントはサブエージェントの結果を盲信せず、主要な主張を対象ファイルと検証で確認する。編集と最終回答は親エージェントが担当する。
- 並列作業を開始したら、完了を個別に何度も問い合わせず、利用可能な待機機構でまとめて待つ。変化のない状態を繰り返し報告しない。

## 長時間作業と引き継ぎ

- 1回で大きな機能を実装しようとせず、受け入れ条件を検証可能な小さな作業単位へ分ける。
- 複数セッションにまたがる場合は、プロジェクト既存の進捗ファイルを優先し、なければ `templates/codex-progress.md` を元に `work/codex-progress.md` を作る。
- 引き継ぎには、目的、完了、現在の状態、次の最小タスク、実行済み検証、判断理由、未解決点だけを残す。機密情報や巨大なログを保存しない。
- セッション終了時は、次のエージェントが開始できる清潔な状態を目指す。中途半端な変更、無関係な生成物、未説明の失敗を残さない。

## 任意 skill の使い分け

- `repo-research`: 未知のリポジトリ、複雑な依存関係、外部仕様の事前調査が必要なときだけ使う。通常の小規模実装では使わない。
- `reverse-engineering`: 既存のブラックボックス、レガシー実装、プロトコル、互換性の挙動を、対象を扱う権限の範囲で解析するときだけ使う。通常のコード読解やデバッグの名前として使わない。
- `github-operations`: GitHubへの明示的な操作が依頼されたときだけ使う。複数ファイルはローカルGitの一括コミット、単一リモートファイルは最新SHAを使ったconnector更新を基本とする。
- skill の指示が OpenAI の標準ルール、ユーザーの明示的な制約、リポジトリ固有ルールと衝突する場合は、後者を優先する。

## 安全境界

- 秘密情報、秘密鍵、トークン、個人情報を出力、コミット、外部サービスへ送信しない。必要なら値を伏せ、存在だけを記録する。
- 依頼のないデプロイ、PR・Issue・外部メッセージ、課金を伴う API 呼び出し、データ削除、権限変更、依存関係の大規模更新を行わない。
- 外部サイトを参照するのは、依頼、仕様確認、または時点依存の事実の検証に必要な場合だけにする。参照した場合は URL と、実装判断に使った事実を記録する。
- リバースエンジニアリングでは、アクセス制御・ライセンス・認証の回避、秘密の抽出、第三者システムへの侵入、持続化や破壊を目的とする手順を扱わない。権限のある対象の互換性・障害解析・防御目的に限定する。
